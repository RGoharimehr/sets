# Core scientific computing
numpy==1.21.5
pandas==1.3.5
matplotlib==3.5.3
seaborn==0.12.2

# Machine learning
torch==1.12.1

# Utilities
pyyaml==6.0
tqdm==4.64.1

# Build dependencies
cmake==3.22.1
pybind11==2.10.1

# Note: The following system packages must be installed separately:
# - build-essential (sudo apt install build-essential)
# - libeigen3-dev (sudo apt install libeigen3-dev)
# - libyaml-dev (sudo apt install libyaml-dev)
#
# For MOSEK (optional optimization solver):
# - Mosek must be installed via conda: conda install -c mosek mosek
# - Or download from https://www.mosek.com/
